import * as React from 'react';

const styles = require('../scss/answer.scss');

export default class AnswerStart extends React.Component {
  render(){
    return (''
      // <div className=''>{this.props.children}</div>
    );
  }
}